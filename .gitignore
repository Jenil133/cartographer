node_modules/
dist/
.env
# Maps are run artifacts. Phase 2 commits curated maps of real apps deliberately;
# until then nothing here should be mistaken for a published result.
maps/
*.log
.DS_Store
examples/*/shop.db

# Private build specs - strategy, not shipped docs.
# NOT all of docs/: phases 2-3 add public docs there (getting-started, mcp-tools, benchmark).
docs/phase-*.md
vision-overlay.png
.carto-shot.png
